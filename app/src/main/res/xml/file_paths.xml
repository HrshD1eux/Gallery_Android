<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Exposes only the subfolder 'shared_images' in cache directory for secure sharing -->
    <cache-path name="shared_images" path="shared_images/" />
</paths>
